query GetIssueById($id: ID!) {
    node(id: $id) {
        ... on Issue {
            id
            number
            title
            body
            createdAt
            state
            url
            author {
                login
            }
            repository {
                id
            }
        }
    }
}